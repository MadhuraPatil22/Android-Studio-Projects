<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="MyApplication"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">

        <!-- ✅ MAIN LAUNCHER ACTIVITY -->
        <activity
            android:name=".LayoutDemoActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- ✅ ALL OTHER ACTIVITIES -->
        <activity android:name=".LinearActivity" android:exported="false"/>
        <activity android:name=".RelativeActivity" android:exported="false"/>
        <activity android:name=".ConstraintActivity" android:exported="false"/>
        <activity android:name=".FrameActivity" android:exported="false"/>
        <activity android:name=".TableActivity" android:exported="false"/>
        <activity android:name=".GridActivity" android:exported="false"/>
        <activity android:name=".AbsoluteActivity" android:exported="false"/>

        <!-- OPTIONAL (if used in your project) -->
        <activity android:name=".MainActivity" android:exported="false"/>
        <activity android:name=".ProfileActivity" android:exported="false"/>

    </application>

</manifest>